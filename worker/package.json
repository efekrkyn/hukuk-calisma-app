{
  "name": "worker",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "for f in $(find src -name '*.test.ts' | sort); do echo \"→ $f\"; tsx \"$f\" || exit 1; done"
  },
  "packageManager": "pnpm@10.10.0",
  "dependencies": {
    "hono": "^4.12.22",
    "ts-fsrs": "^5.4.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260524.1",
    "tsx": "^4.23.12",
    "typescript": "^5.9.3",
    "wrangler": "^4.94.0"
  }
}
