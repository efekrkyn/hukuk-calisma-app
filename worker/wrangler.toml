name = "hukuk-worker"
main = "src/index.ts"
compatibility_date = "2026-05-01"
compatibility_flags = ["nodejs_compat"]

[observability]
enabled = true

# D1 binding — Task 6'da doldurulacak (wrangler d1 create hukuk-db sonrası)
# [[d1_databases]]
# binding = "DB"
# database_name = "hukuk-db"
# database_id = "<EKLE>"

# R2 binding — Task 7'de aktive edilecek
# [[r2_buckets]]
# binding = "PDF_BUCKET"
# bucket_name = "hukuk-pdf"
